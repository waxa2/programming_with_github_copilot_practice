This repository intends to gather the practice exercises contained in the *Programming with GitHub Copilot* by Kurt Dowswell.

# Todo API

The `todo-api` project is a RESTful API for managing a to-do list. It allows users to create, read, update, and delete tasks. This project is designed to be simple, efficient, and easy to integrate with other applications.

## Features

- Create a new to-do item
- Retrieve all to-do items
- Retrieve a specific to-do item by ID
- Update an existing to-do item
- Delete a to-do item

## Prerequisites

Before running the project, ensure you have the following installed:

- [Node.js](https://nodejs.org/) (v14 or higher)
- [npm](https://www.npmjs.com/) or [yarn](https://yarnpkg.com/)

## Installation

1. Clone the repository:

   ```bash
   git clone <repository-url>
   cd todo-api
